$('.preached-btn').on('ajax:success', function(){
	$(this).parent().parent().find('.like_count')
	.html('<%= @post.get_likes.size%>')
	$(this).closest('.preached-btn').html('<i class="far fa-heart"></i>')
});
$('.preach-btn').on('ajax:success', function(){
	$(this).parent().parent().find('.like_count')
	.html('<%= @post.get_likes.size%>')
	$(this).closest('.preach-btn').html('<i class="fas fa-heart"></i>')
});